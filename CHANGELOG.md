# Changelog

本项目的重要变更记录在此文件中。

## [0.2.0] - 2026-09-20

### Added

- 新增 LLM 意图路由器（ADR-0009）：关键词未命中的对话轮改经意图路由补齐，RouterTrace 持久化到 Postgres 并统一回复护栏。
- 新增运营台 JWT 登录与 RBAC（ADR-0010）：用户名 + 密码登录、首登强制改密、15 分钟访问令牌与单次轮换刷新令牌；个人令牌保留为机器通道供评测/脚本使用。
- 新增 100 例对话评测套件、控制台真实数据页 E2E 与控制台↔Go 线上契约 golden 测试。
- 新增交付卫生工具：迁移序号 lint（重复数字前缀防再犯）、根 .env.example（docker-compose 变量全集）与 tests/unit 接入评测链。

### Changed

- 导播台重写：operator.html#live 组件化进控制台（ADR-0011）并退役 operator.html（ADR-0013），28 个导播评测迁至新导播页。
- 五个手写 LLM 客户端统一到同一 openaicompat 传输（ADR-0012）；最大的两个服务端面拆出 main.go，两个纯引擎从 agent.go 释放。
- 三轮架构与前端打磨：契约 golden、传输缝抽取、match_screen 拆分出可测试模块、观察存储收敛到纯 reducer 并加双存储一致性测试、记忆积压优先排水。
- 客户端断线重连后补一次比赛概览再同步（幂等 GET、非轮询）；release 构建缺失 matchId / WS 地址配置时进入明确错误空态，不再静默连向模拟器兜底地址。
- MetalButton 对齐 DESIGN.md：纯色 championBlue 填充 + 8px 圆角，弃用三段渐变。

### Fixed

- 修复表演链路：hello 开场守卫、旧表演路径移除、进球爆屏按事件 ID 边沿触发（重建不重放）。
- 修复记忆管道：写入失败即移交持久积压、ForgetPortrait 如实上抛、缓存上限，避免静默丢失。
- 修复匿名身份保管：deviceId 迁入安全存储并支持重建复用，隐私删除后如实重开全新身份。
- 修复客户端韧性：音频源生命周期释放、WS 半开连接自愈、重连语音回退不再遗留队列。

## [0.1.0.0] - 2026-07-15

### Added

- 新增面向普通用户的数字球友客户端，支持 Live2D、连续语音对话、首见问候、比赛状态轮播与文字降级。
- 新增企业导播台，支持人工比赛事实录入、外部数据源接管、自动化策略、实时监控与决策追踪。
- 新增比赛事实保护、用户错误赛况核验、来源冲突处理及已确认事件状态。
- 新增跨比赛关系状态、共同瞬间、未完话题、边界、修复和长期偏爱记忆。
- 新增发布级离线、WebSocket、浏览器和真实 MiMo 语音评测体系。

### Changed

- 重构后端对话链路，将比赛事实、关系导演、回复实现、语音和 Live2D 表达统一到同一交付流程。
- 重构导播台的信息架构，合并重复入口并明确人工与自动数据源的职责。
- 完善 Docker、Flutter Web、Android、CI 和本地演示运行配置。

### Fixed

- 修复导播鉴权、音频播放回执竞态、语音状态文案和用户意图识别问题。
- 修复主动比赛信息与用户输入的排序，避免低优先级事件打断用户对话。
- 修复关系记忆在发送失败、跳过、断线及回执抢跑时的状态残留和重复召回。
- 修复用户端暴露内部术语、比赛事实未经确认写入共同记忆及回复模板化问题。

### Removed

- 移除未接入生产链路的旧 pipeline engine、cooldown、dedup、priority、throttle 和 watch session 实现。

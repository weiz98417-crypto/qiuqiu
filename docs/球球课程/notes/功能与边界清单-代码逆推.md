# 球球项目 功能与边界清单(代码逆推,2026-09-20)

> 内部事实源素材。由代码全量扫描逆推,作为文档覆盖审计与产品资料补全的基准。路径均为仓库根相对路径。

## 一、观赛旅程(进入/赛前/赛中/赛后/收束)

| 能力 | 用户可感知行为 | 边界与失败行为 | 实现位置 |
|---|---|---|---|
| 匿名身份 | 首次访问自动建匿名 userId,存 secure storage,迁移永不换 UUID | 无服务端账号;凭证本地持有 | client/lib/services/preferences_service.dart |
| 会话建立 | POST /api/sessions/anonymous 签发访问+刷新令牌;refresh/revoke | production 强制 session 模式;签名密钥≥32 字符 | backend/cmd/server/session_api.go; backend/internal/auth/ |
| 比赛目录 | 目录页列出 scheduled/live/finished 比赛,可搜索+筛选 | 只露这三种状态;排序按状态秩 | backend/internal/matchstate/catalog.go; client/lib/screens/match_catalog_screen.dart |
| 深链进入 | ?matchId= 直达比赛;退出清参,刷新回列表 | 刷新不回比赛 | client/lib/screens/match_entry_screen.dart; browser_history_web.dart |
| 赛前大厅 | 展示对阵、球员名单(首发/替补)、连接标记,可进房/重连/设置/离开 | overview 加载失败有错误文案与重连 | client/lib/screens/match_lobby.dart; match_overview_service.dart |
| 赛前配置 | 导演设队伍/名单/比分基线,同步事实 API | 保存经后端确认(eval 锁定) | console/src/pages/DirectorLive.tsx; match_operator_api.go |
| 实时接入 | WS /ws/match/{id}:snapshot、clock 轮播、match_event 推送 | Origin 校验、每 IP≤5 连接、读上限 1MB、读写超时 60/10s | watchconnection.go; ws/handler.go; config.go |
| 客户端重连 | 断线自动重连≤5 次,25s 心跳 ping | 超限停止;重连后回放未送达内容 | websocket_service.dart; conversation/watch_session.go |
| 后台时钟 | 比分/时钟由服务端推进;阶段切换清零 | 事件不把主时钟跳回;时钟 PATCH 409 版本冲突后自动重读 | matchstate/clock.go; console/src/director/api.ts |
| 赛中事实流 | 事件(含候选 pending)确认后推公开事实;快照剔除 Integrity | pending 不改公开比分;候选确认只加一次;revision 变化去重保序 | matchstate/fact_ledger.go; operator-control.spec.mjs |
| 事实撤回 | 已撤事实触发的主动回复被 match_fact_retracted 移除 | — | cmd/server/main.go (matchFactRetractedMessage) |
| 外部数据源 | APISports 轮询(默认 3s)自动写事件;可启停/切换/人工接管(接管同时暂停自动播报) | 无 key 则纯人工;源状态按 2×poll+p95 判活 | datasource/manager.go; match_operator_api.go |
| 冲突调和 | 人工与外部源冲突→球球暂停确认赛况;候选卡逐项调和,默认保留兼容事实 | 采用候选需更正原因;保留原事实时用户端比分不抖 | matchstate/conflict_resolution.go |
| 用户纠错主张 | 用户报比分/进球不改事实,只记 pending 观察并回复核验姿态 | 重复同一主张走"温热保持"固定话术(不重启观察) | companion/matchclaim.go; observation_hold.go; observation/coordinator.go |
| 观察跟进 | 确认/撤销后只跟进对应用户;离线确认重连后补跟进,展示后不重复 | follow-up 有 deadline,过期不补;可被 in-band 刷新抑制 | observation/coordinator.go; watchconnection.go |
| 完场收束 | fulltime 触发告别:wave 动作+告别语只用一次,随后回待机 | — | fulltime-farewell.spec.mjs; relationship/policy.go |
| 赛后记忆 | match_end 入队 post_match 反思(画像刷新+审计);空闲每 15min idle 反思 | 反思失败只记日志,不碰回复 | main.go (runReflectionBeat) |
| 语音播报 | TTS mimo-v2.5-tts 固定音色(默认"冰糖"),按 presentation 注入表演指令(style/energy/speed) | TTS 失败→字幕兜底;开发环境 QIUQIU_RUNTIME_TTS=1 才有 mock 音频 | tts_style.go; main.go |
| 自动播放 | 浏览器拦截首屏语音后,首次触碰恢复播放;播放回执五态入账 | blocked 记为 skipped 类 | client-voice-runtime.spec.mjs; playbackTraceStatus |
| 送达去重 | 客户端按 deliveryKey 去重,保留 fact revision 变化;服务端 TTL 去重 | — | client-delivery-dedupe.spec.mjs; signal_deduper.go |
| 断线语音补发 | 重连后排队补发语音兜底,队列上限 4 条(先进先出淘汰) | 未连接不发 | match_session_controller.dart |

## 二、对话与表达

| 能力 | 行为 | 边界 | 位置 |
|---|---|---|---|
| 意图分类 | 关键词 12 意图(smalltalk/schedule/match_status/recent_event/match_reaction/follow_up/player_question/emotion_reaction/personal_share/control_command/match_fact_claim/unknown) | 改比分/记进球类注入词强制归 unknown | companion/classify.go |
| LLM 意图路由 | 关键词 miss 走 mimo-v2.5 路由(意图+置信+建议回复);ROUTER_API_KEY 缺省回落 MIMO_API_KEY,双无则整层关闭 | 低置信控制指令被拒;unknown 无建议则固定追问 | router/router.go; intent_router.go |
| 表达实现 | LLM realizer(5s 超时)把决策转成口语;provider 失败回落确定性话术 | 锚定纪律:回复不得超出锚源 | companion/agent.go; polisher-*.json |
| 护栏 | 句数/字数上限、ForbiddenClaims、反重复;两路径共用 | 拒绝原因落 trace | companion/guard.go; reason_codes.go |
| 短语冷却 | 回复前读最近 12 轮做措辞去重 | — | companion/agent.go |
| 主动发言门禁 | 同时要求:自动化 active+事件白名单+非空引用+档位不禁止 | quiet 只抑制不禁用 critical;normal 冷却 90s(0-300 可配),active×0.6 | conversation/proactive_gate.go; talkativeness.go |
| 主动 TTL | critical 45s / normal 12s 过期即弃;关系层可覆写 | — | main.go (proactiveTTL) |
| 打断 | 用户说话/interrupt 取消排程+打断播报,回 ack+被打断反应 | — | watchconnection.go; scheduler.go |
| 话痨档位 | quiet/normal/active,WS set_talkativeness 即时持久化+ack;重连恢复持久档 | ack 为真源;console 只读 | watchconnection.go; console_api.go |
| 语音输入 | 按住说话(VAD 0.015/0.006)+流式 asr_start/chunk/finish/cancel;每连接≤8 并发转写 | ASR 失败→text_fallback/音频兜底;麦克风拔出回落默认 | vad_service.dart; streaming_transcription.dart; transcription.go |
| 文字降级 | 麦克风未授权保留文字路径 | — | workflows.spec.mjs |
| 表情/动作映射 | PresentationPlan:13 表情×12+动作组+voice style;holdMs 默认 1800(clamp 0-10000) | 服务端白名单+别名归一,非法名落 idle | presentation_table.go; presentation_state.dart |
| 待机三档 | affect 映射 deflated/calm/energetic;30s 重挑,60s 切档锁,0.1 迟滞;**首次永不 energetic** | — | idle_tier_picker.dart |
| 情感状态机 | 事件驱动 valence/arousal/tension/confidence/engagement,2min 衰减回 0.6 基线 | clamp 限幅 | relationship/affect.go |
| 关系动作词表 | ack/analyze/chat/ask/disagree/opinion/recall/react/repair/silence/tease→表情语音;同信号幂等 | critical fact revision 允许一次刷新 | relationship/types.go; director.go |
| 首次见面 | first_meeting(昵称+主队)30s TTL 排程,一次投递 | 重复发不再播 | watchconnection.go |
| 赛程渐进查询 | ack→上下文→结果三段式;今天/明天/现在 scope;带时区;被打断即取消 | 数据源无日期范围能力时报 unavailable | classify.go; schedule_lookup.go |
| 控制指令 | "别说/少说/闭嘴"归 control command,accepted 走降档 | — | classify.go; route-control-*.json |

## 三、记忆与隐私

| 能力 | 行为 | 边界 | 位置 |
|---|---|---|---|
| 交互账本 | 追加式 ledger 七类事件 | 唯一本地事实源;Memobase 永不写 Match Facts | interaction/ledger.go |
| 记忆抽取 | 异步队列(容量 256)观察→Memobase;积压重试≤10;审计表记抽取/拒绝 | 无数据库降级 Ledger-only | memory/queue.go; memobase.go |
| 画像(球球懂我) | Memobase+本地 overlay 按 (topic,sub_topic) 确定序合并;读/改/忘(删除即 tombstone,下回合生效) | overlay 是权威覆盖层 | memory/portrait.go; portrait_postgres.go; portrait_api.go |
| 画像一致性 | 已删条目不得再被回话引用 | eval 回归锁定 | portrait-consistency.json |
| 开放话题 | 未答问题/承诺/情绪时刻/预测入台账;TTL 7 天过期(过期≠已答,分别写审计);主动发言须引用 thread 或共同时刻 | 2min ticker | memory/threads.go; proactive_gate.go |
| 话题恢复 | memory.recover_thread 从已录事实回答历史未答问题 | — | companion/boundary.go |
| 话痨偏好持久化 | Postgres 记录档位,console 只读 | console 无写入口(红线) | memory/preferences.go |
| 隐私导出 | GET /api/me/export 导出 11 类集合 | 需会话 bearer | privacy/service.go (Export) |
| 删除与保留期 | DELETE /api/me/data 起删除 job(可按 jobID 查);过期数据每 6h 清理;默认保留 30 天 | retention≤0 拒绝启动 | privacy/service.go; runPrivacyCleanup |
| 匿名说明页 | 未登录 GET /api/me/privacy 返回公开保留说明 | — | privacy_api.go |
| 记忆健康 | overview 暴露积压深度+降级徽标 | — | console Overview.tsx |

## 四、运营与运维

| 能力 | 行为 | 边界 | 位置 |
|---|---|---|---|
| 双通道登录 | 账号密码(JWT+首登强制改密)+机令牌通道 | 必须配 QIUQIU_JWT_SECRET;临时密码只显示一次 | consoleauth/; LoginPage.tsx |
| 角色 scope | director:match:write/fact:confirm/fact:correct/trace:read;auditor 仅 trace:read(写按钮隐藏) | 吊销=删行立即失效 | operatorauth/; Operators.tsx |
| 引导运营员 | QIUQIU_BOOTSTRAP_OPERATOR 空表播种首个 director | — | main.go |
| 幂等写 | 全部运营写按 Idempotency-Key 幂等,带 busy 态 | — | operatorwrite/ |
| 概览六格 | 活跃比赛/在线会话/话题老化/记忆健康/最近主动引用/词汇漏斗(24h unknown 率) | — | Overview.tsx; console_golden_test.go |
| 比赛页 | 事件流+用户网格+审计轨迹+设置(赛前配置、自动化范围+冷却、数据源与接管、生命周期) | auditor 只读 | Match.tsx; MatchSettings.tsx |
| 导演直播页 | 行为条 18 类事件只写草稿;草稿卡选人;语音草稿(ASR+LLM 抽取,冲突逐项处理后发布);事实时间线 | draft-only 绝不直发;goal 自动+1 只加一次;score_correction 静默不触发主动话术;goal_cancelled 回退比分;VAR 必须引用被审事实 | console/src/director/* |
| 比分更正审计 | correct+更正原因作为独立审计事实 | 无原因拒绝 | event-model.ts |
| 时钟治理 | 主时钟后端推进;PATCH 带版本,409 冲突重读自愈 | 事件不回拨时钟 | clock.go |
| 引用审计 | 按 citation 前缀深查 trace;"为什么说话"抽屉 | 未填 matchId 不发请求 | CitationAudit.tsx |
| 用户代客操作 | 画像只读、话题标记已答/过期、交互历史 | 画像删=console DELETE portrait;无 talkativeness 写 | User.tsx; Threads.tsx |
| 中断反应流 | 服务端 ring 收集全部 interrupted 反应,console 只读展示 | — | delivery.go |
| 静态托管 | / 托 Flutter web;/console 托 Vite dist;dev 专页仅非 production | — | main.go |
| 演示重置 | demo/test 比赛白名单重置,清 traces+关系+观察 | 非演示 ID 拒绝 | isDemoMatchID |
| 事实投影影子审计 | legacy vs projected 比分差异按签名去重打日志 | 仅日志 | main.go |
| 备份 | postgres_data 卷=唯一全量事实源;pg_dump 每日 03:30+恢复步骤;memobase 库可重建 | — | deploy/README.md |
| 拓扑 | backend+postgres(pgvector)+redis+memobase(独立 pg+redis);production 必需四环境变量 | — | docker-compose.yml; config.go |

## 五、评测锁定的行为

Go 套件 79 例:baseline 36(比分/进球者/助攻/赛程/情绪/闲聊/感谢/控制/claims)、boundary 31(梦进球/假设/玩笑/坚持副词/注入改分/同音字/长乱码/低置信路由/控制接受)、regression 12(open-thread 恢复、realizer 越锚/回落、画像一致性、非疑问句漏斗、persisted-claim 三连、用户隔离、手动主动线)。关系旅程期望(stage/repair/actions/记忆种类)。tests/evals 18 条 Playwright spec 共 5517 行:operator-control 28 例、console 六页契约、auth、identity、threads、director(409 自愈)、workflows 12 例、observation 3 例、fact-lifecycle、fulltime-farewell、goal-follow-up、intent-unknown、phase-motions、vad/microphone/voice-runtime/delivery-dedupe、connection-check。Golden payload 13 文件逐字锁定 console 契约。

## 六、易被文档遗漏的冷门能力(审计重点)

重连语音兜底队列上限 4 条淘汰最旧;in-band 关键事实刷新(语音回合前后比分修订则同文本重生成,观察跟进标 suppressed_in_band);用户重复主张不算第二来源(温热保持固定话术);主动 TTL 45s/12s 双档;待机首挑永不 energetic;holdMs 服务端可配客户端 clamp;TTS 表演指令拼贴(固定人设长指令+style/energy/speed 中文方向词);每连接转写会话上限 8;WS 每 IP 5 连接/1MB/双心跳;开发环境才注入 mock TTS 否则纯字幕;事实投影影子审计仅日志;比分更正静默不触发主动话术;录音回执五态映射 ledger;interrupted 反应服务端环形收集+console 只读 feed;客户端时区影响赛程窗口;匿名身份迁移绝不重生成 UUID;演示白名单重置清关系与观察;赛程日期范围缺失时报 unavailable;console 话痨档位刻意只读(与用户侧不对称);Live2D 资产 CORS 全开+operator-live-state.js no-cache 特判;话题过期≠已答分别审计;意图路由双 key 回落;记忆积压深度+降级徽标直出概览;深链退出清参防"刷新回赛";idle 档与后端 affect 基线数值耦合。
